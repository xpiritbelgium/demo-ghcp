name: Deploy Infra and App
on:
  push:
    branches:
    - main

permissions:
  id-token: write
  contents: read

jobs:
  DeployDev:
    name: Build and Deploy to DEV
    runs-on: ubuntu-latest
    environment: DEV

    steps:
      - uses: actions/checkout@v2

      - name: Azure Login
        uses: Azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy Resource Group
        uses: Azure/arm-deploy@v1
        with:
          scope: subscription
          region: westeurope
          subscriptionId: '${{ secrets.AZURE_CREDENTIALS.subscriptionId }}'
          template: ./iac/resourcegroup.bicep
          parameters: 'resourceGroupName=rg_cleanarchitecture_dev location=westeurope'

      - name: Deploy Azure Services
        uses: Azure/arm-deploy@v1
        with:
          resourceGroupName: rg_cleanarchitecture_dev
          template: ./iac/main.bicep
          parameters: 'sku=S1 location=westeurope sqlpassword=${{ secrets.AZURE_SQL_PASSWORD }}'

      # - name: build, deploy, 
  
